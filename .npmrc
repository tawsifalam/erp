# Puppeteer (via md-to-pdf): postinstall is skipped via package.json → pnpm.ignoredBuiltDependencies.
# PDF scripts use system Chrome or Playwright — docs/visual/README.md
# Manual override: PUPPETEER_SKIP_DOWNLOAD=true pnpm install
