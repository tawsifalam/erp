# PRD: QR ordering

## Scope

Guest-facing PWA at `/order/:tableToken` — menu browse, cart, submit to POS without cashier login.

## Security

Signed table tokens per branch; rate limits; no PII required for walk-in F&B.
