# Prisma and `pnpm db:*` read DATABASE_URL from the repo root `.env` (see prisma.config.ts).
# Copy the root file instead of maintaining a duplicate here:
#
#   cp ../../.env.example ../../.env
#
# Optional: create apps/api/.env only for API-only overrides (loaded after root).
