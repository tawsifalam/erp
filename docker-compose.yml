# Run from repo root. All api/web config lives in ./.env (see .env.example).
#   docker compose build
#   docker compose up -d
#
# With -f infra/docker/... only, pass: docker compose --env-file .env ...

include:
  - path: ./infra/docker/docker-compose.yml
